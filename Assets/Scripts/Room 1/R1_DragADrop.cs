using UnityEngine;

public class R1_DragADrop : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    GameObject gameOject;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
